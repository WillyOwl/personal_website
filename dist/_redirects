# Netlify redirects configuration
# Serve static files from composition-assistant directory first
/composition-assistant/* /composition-assistant/:splat 200

# For all other routes, serve the React SPA (except static assets)
/* /index.html 200
